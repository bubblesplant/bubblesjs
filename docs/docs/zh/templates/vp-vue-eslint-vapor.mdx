import { CommandPanel, StackList } from '@/components/docs-ui'

export const stack = [
  'Vue 3.6 RC、Vapor 模式、Vue Router 5、Pinia 4。',
  'Vite+ 0.2、Vite 8、TypeScript 6、Vue TSC。',
  'Antdv Next、ECharts、UnoCSS、Sass、SVG 图标插件。',
  'Alova 与 Axios 适配器。',
  'ESLint、Oxlint、Vite+ staged checks。',
]

export const commands = [
  'pnpm install',
  'pnpm dev',
  'pnpm build',
  'pnpm preview',
  'pnpm lint',
  'pnpm lint:fix',
]

# vp-vue-eslint-vapor

基于 Vite+ 的 Vue Vapor 应用模板，用于体验 Vue 3.6 的 Vapor 编译模式。

## 技术栈

<StackList items={stack} />

## 命令

<CommandPanel commands={commands} />

## 注意

模板通过 `@vitejs/plugin-vue` 的 `features.vapor` 开启 Vapor 模式，并使用 Vue 3.6 RC。它更适合功能验证和提前适配；对稳定性要求较高的生产项目建议选择稳定版 Vue 模板。
