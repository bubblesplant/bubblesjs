# 前端国际化测试报告

## 验证范围

- 应用 locale 到 antd `ConfigProvider` 的映射。
- 应用 locale 到 Pro Components `IntlType` 的映射。
- 应用 locale 到 Day.js 和 `<html lang>` 的同步。
- 词条完整性、TypeScript 编译和 React Web 测试回归。

## 验证结果（2026-09-19）

| 命令 | 结果 |
| --- | --- |
| `pnpm --filter web build` | 通过，TypeScript 与 Vite 构建成功 |
| `pnpm --filter web test --run` | 通过，17 个测试文件、112 个测试 |
| `pnpm i18n:check` | 通过，1254 个词条无新增、废弃或未使用 |
| `git diff --check` | 通过 |

本次未改动后端接口、权限契约或 UI 截图；浏览器视觉回归仍沿用主题验证说明中的既有流程。
