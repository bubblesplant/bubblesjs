# 前端国际化交付记录

## 交付内容

- 在 `apps/web/src/i18n/config.ts` 集中声明应用 locale 到组件库和日期库的标识映射。
- 新增 `apps/web/src/i18n/ui-locale.ts`，统一返回 antd Locale、Pro Components `IntlType`、Day.js 和 `<html lang>` 配置。
- `apps/web/src/App.tsx` 根据同一 locale 状态驱动 `ConfigProvider`、`ProConfigProvider` 和 Day.js。
- 更新 Web README 与 `.spaces` 正式需求、设计、测试和索引文档。

## 状态

已实现并完成构建、测试和 i18n 词条检查。当前语言范围仍为 `zh_CN` 与 `en_US`。
