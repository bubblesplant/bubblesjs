# 前端实现与验证交接

> 文档状态：历史基线＋当前证据边界。企业邀请只确认产品入口，固定协议仍待产品确认。

历史交接日期：2026-09-10。前端实现以已确认需求及 [技术契约](./技术契约.md) 为准，业务代码修改范围仅为 `apps/web`。2026-09-17 成员接入契约已经变更，原交接与测试结果仅作为历史基线，不能证明当前成员入口已经实现或通过。

## 2026-09-18 当前口径与待复验范围

- 平台创建企业、设置或更换企业管理员统一使用 `GlobalAccountSelect`，表单保存 `userId` 并提交 `administratorUserId`，不再把账号字符串作为关系标识。
- 企业成员页当前已确认的新增能力是“直接注册新账号并加入企业”，提交姓名、完整账号和密码；不能继续使用完整账号搜索并直接拉入既有平台账号。
- 企业邀请只确认是企业新增成员的另一条产品入口；邀请对象、载体、有效期、状态、接受／拒绝、撤销／重发、安全与审计规则均待确认。
- 当前前端已存在 7 天链接管理和 `/member-invitations/accept?token=...` 接受页，但这是未获产品确认的实现记录，不是现行交接要求，不得据此继续扩展或宣称产品可用。
- 项目成员页使用 `OrganizationMemberSelector` 的 `purpose=addProjectMember`，只展示所属企业有效且未加入当前项目的成员，选择和提交均为 `userId`；不展示组织、岗位或角色标签。
- 管理员选择、企业直接注册和项目成员候选已有组件／请求层自动化与构建证据，但当前真实浏览器全过程和正式环境仍未复验。邀请相关测试只能证明现有实现行为，不能替代产品确认或产品验收。

## 实现范围

- `src/pages/register`、`src/pages/login/index.tsx`：公开注册、登录、注册成功账号提示，以及登录后的工作空间入口。
- `src/pages/workspaces`：按服务端有效身份展示企业、项目和平台工作空间。无企业普通账号不得被要求提供完整账号供管理员直接挂接；具体邀请引导文案应在协议确认后定稿。有效平台身份仍保留平台入口。
- `src/pages/access`：工作台、企业开通与启停、项目创建与启停、管理员补任与更换、账号与成员管理、角色分配、自定义角色与权限树、资料、三套菜单编辑、废弃权限清理预览与执行、审计筛选与分页。当前邀请列表／创建／撤销／重发界面仅作为未确认实现登记。
- `src/pages/member-invitations`：当前存在基于 URL token 的登录／注册和主动确认页面，仅作为未确认实现登记；产品协议确认前不把其路由、状态或交互当作稳定契约。
- `src/pages/access/components`：通过 `show/hide` 暴露弹窗操作，所有写操作使用共享契约 DTO。内置角色定义只读；权限树保留页面和操作的依赖关系及全选、半选行为。
- `src/router`：17 个静态管理页面注册，路由按功能拆入 `modules`，父级 scope middleware 获取作用域上下文，叶子 access middleware 校验页面权限；未知页面显示 404。页面 403 不会清空其他可用导航；空间首页无读权限时，优先进入可见菜单中的已授权静态页面。默认入口对无平台或额外企业职责的单项目成员直接进入项目，其余进入工作空间；明确选择的工作空间和页面不被分流覆盖。示例页面不出现在生产菜单。
- `src/router/lazy-load`、`src/App.tsx`：使用 `lazyLoad('access', 'home')` 等相对 pages 的路径统一懒加载，并在 App 中只保留一个 `RouteTransition`。
- `src/layouts/WorkspaceLayout/index.tsx`、`workspace.css`：三种作用域布局、具体企业／项目名称、导航和切换；菜单路由等待期间保留旧页面视觉内容并禁用交互，由加载层覆盖，目标路由提交后再替换以保持 `RouteTransition` 动画；进入新空间时重建业务子树；权限刷新时隐藏页面并保留表单输入。
- `src/utils/request/workspace.ts`：沿用现有 alova 请求封装；管理请求禁止缓存，取消旧请求，使用用户、作用域和请求代次阻止迟到结果。旧 401 不得退出新账号，旧 403 不得刷新新空间。当前业务 403 刷新一次，access 403 不递归。
- `test/workspace-requests.spec.ts`、`test/workspace-navigation.spec.ts`、`test/router`：权限导航、请求竞态、入口分流及真实 React Router memory 导航回归测试；原 loader 测试迁移至 middleware 测试。

## 2026-09-10 middleware 简化历史验证

- 移除根 `navigationMiddleware`、`router.subscribe` 和导航快照桥。具体 middleware 通过原生 Router context 交接校验数据；布局从 `pages/workspaces/state.ts` 按当前空间读取权限，再通过 `Outlet context` 传给页面。状态按登录会话隔离，每次导航仍请求最新权限。
- Web 全量 `vp test`：9 个文件、81 项通过。原快照桥的 10 项实现测试替换为 6 项工作空间行为测试，入口及权限的 28 项测试继续保留；请求层的取消、换号和迟到响应测试不变。
- 测试 TypeScript 检查、Web TypeScript／Vite 构建及本次 14 个代码文件的定向 `vp check` 通过。
- 浏览器使用本机前端和拦截 API 响应验证：登录单项目直达、唯一过渡容器、叶子拒绝保留父布局、同位置刷新名称、页面撤权与恢复、整个空间撤权清除布局与重试恢复、慢速项目切换、手动选择及刷新为空列表。无页面运行时异常；本轮未连接真实后端。

## 2026-09-10 middleware 重构历史验证（简化前）

- Web 全量 `vp test`：9 个测试文件、85 项测试通过，其中 `test/router` 的 38 项覆盖默认入口、权限边界、导航取消、换号、刷新和 Fetcher 隔离。
- `vp run --filter web build`：TypeScript 与 Vite 构建通过。
- 浏览器使用本机前端和拦截 API 响应验证：登录后单项目直达、全局唯一过渡容器、手动选择工作空间、明确企业目标、Hash 导航、叶子 403 保留父布局、作用域撤权清除布局、重新加载恢复、再次撤权及返回空工作空间；无页面运行时异常。本轮未连接真实后端。
- 仓库全量 `vp check` 仍被其他文件的格式问题阻挡；本次范围单独执行检查，不对全仓执行格式修复。

## 2026-09-09 验证记录

以下保留 2026-09-09 的实际验证记录，命令、文件名和数量反映当时实现；不作为后续 middleware 重构的验证结果：

| 命令                                                                                                                                                                                                                                                                                                                            | 结果                                                                                                                                           |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| `vp run --filter web build`                                                                                                                                                                                                                                                                                                     | 通过。TypeScript 检查完成，Vite 构建 4494 个模块。现有 ProTable 相关产物存在大于 500 kB 的 chunk 提示，没有通过提高阈值隐藏提示。              |
| `vp run --filter web test`                                                                                                                                                                                                                                                                                                      | 6 个测试文件、43 项测试全部通过：storage 14、request 11、draft-projects 4、workspace-requests 5、workspace-navigation 6、workspace-loaders 3。 |
| `vp check apps/web/src/utils/request/workspace.ts apps/web/src/pages/access apps/web/src/pages/register apps/web/src/pages/workspaces apps/web/src/router apps/web/src/layouts/WorkspaceLayout.tsx apps/web/test/workspace-requests.spec.ts apps/web/test/workspace-navigation.spec.ts apps/web/test/workspace-loaders.spec.ts` | 31 个文件格式正确，无 lint 警告、错误或类型错误。                                                                                              |
| `git diff --check -- apps/web`                                                                                                                                                                                                                                                                                                  | 无空白错误。Windows Git 的 LF／CRLF 提示不影响此结论。                                                                                         |

14 项新增测试覆盖：切换取消与迟到成功；换号后的旧 401；旧作用域 403；当前 403 单次刷新与 access 防递归；Router AbortSignal；未知菜单、所属操作和空目录过滤且不改源数据；隐藏／停用导航与静态路由；未知／跨作用域 routeKey；服务端允许图标的完整前端映射；可见授权页面优先落点；没有受支持页面时无落点；叶子 403 保留父级上下文且可进入其他管理页；首页停用后自动重定向；整个作用域身份撤销后不保留旧上下文。

最终检查前修复了菜单平铺后的父级选择问题：编辑器从原始树定位当前节点并排除自身及全部后代，防止平铺行的空 children 漏掉后代选项。

联调复查发现并修复资料保存后的本地版本未递增问题：保存成功合并服务端返回的最新资料及 version，后续编辑使用最新版本；真正版本冲突仍保留输入。该修复已重新通过完整 TypeScript／Vite 构建及 31 文件检查，连续保存的真实浏览器验证交由 QA。

## 联调边界

- 页面直接调用真实 API，没有使用 mock 数据。共享类型与纯工具统一从 `shared/types`、`shared/utils` 导入。
- 企业项目列表的管理员设置使用已同步契约的 `GET /companies/:companyId/projects/:projectId/administrators`，支持读取停用项目的显式项目管理员，避免将企业隐式管理员当作待替换的项目身份。
- React Router 当前安装版本为 8.3.1，使用原生 Data Mode middleware。工作空间列表及各空间权限保存在 `pages/workspaces/state.ts`，页面随导航或重新校验完成后读取。布局通过 `Outlet context` 向子页面提供同一份权限；缺数据时显示加载态，避免撤权过渡中的旧页面读取空权限。不再订阅路由状态或维护导航快照。
- 开发前端默认 API 前缀为 `/api`；隔离联调环境由主控启动，前端端口 5301，后端端口 3301。前端代理通过 `VITE_API_URL` 配置。
- Playwright 真实页面、后端接口和数据库事务验收由主控及 QA 独立执行，结果应写入最终测试报告，不能用本交接中的静态检查替代。

## 变更记录

| 日期       | 变更摘要                                                                                                                                         | 影响范围                                 |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------- |
| 2026-09-18 | 修正菜单路由等待态：保留旧页面视觉内容并隔离交互，目标路由提交后再替换以恢复 `RouteTransition` 动画                                               | 工作空间布局、导航等待态与前端回归测试 |
| 2026-09-17 | 增加平台全局账号选择、企业直接注册、邀请入口和项目企业成员候选的前端交接要求；当时写入的固定邀请协议未获产品确认                                 | API、成员页面、选择器、路由、i18n 与测试 |
| 2026-09-18 | 纠偏邀请前端口径：7 天管理界面和接受页降级为未确认实现记录；保留直接注册和项目 `userId` 候选的自动化证据，并明确缺少当前真实浏览器／正式环境复验 | 成员页面、邀请页面、路由与验证状态       |
