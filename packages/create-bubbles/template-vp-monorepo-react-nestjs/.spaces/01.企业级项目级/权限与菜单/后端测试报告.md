# 企业级／项目级后端交接与测试报告

> 文档状态：历史基线＋当前待复验。

历史验收日期：2026-09-09。范围：`apps/server`。测试使用独立本机 PostgreSQL、Redis 与队列，不初始化或清理 QA 的 `enterprise_test` 数据库。2026-09-17 成员接入契约已经变更，原测试结果仅作为历史基线，不能覆盖下述新增复验范围。

## 2026-09-17 增量复验状态

- 平台创建企业和设置企业管理员统一提交 `administratorUserId`，需要复验 GlobalAccountSelect 搜索／回显及服务端有效账号校验。
- 企业成员直接注册需要复验账号、企业成员、默认 member 角色和审计同事务写入，密码不得进入审计、日志或响应。
- 企业邀请只确认是企业新增成员的另一条产品路径，目标范围、是否绑定账号、载体、有效期、状态、接受／拒绝、撤销／重发、发送渠道、DTO、Schema、端点、事务、安全、审计和验收规则均待确认；当前不进入后端增量复验。
- 邀请协议确认后必须先同步需求、PRD 与技术契约，再补数据库、控制器、事务、审计和集成测试，不得按旧草案预建固定实现。
- 项目成员写接口改收 `{ userId }`，需要复验账号有效、所属企业成员有效、尚未加入当前项目三项事务内校验，以及跨企业、停用和重复关系拒绝。
- 以上复验完成前，不把 2026-09-09 的 33/33 结果描述为当前成员入口全部通过。

## 2026-09-09 历史交付范围

- `src/modules/access/`：工作空间、平台企业与账号、企业项目、成员、角色、菜单、审计、管理员补任／替换、废弃权限预览与清理。
- `src/database/access.schema.ts` 与增量迁移 `drizzle/0002_enterprise_access.sql`、对应 snapshot/journal；保留既有迁移。
- 全局权限默认拒绝；公开、仅登录和业务范围策略均需明确声明。平台身份不自动获得企业访问权，企业管理员隐式管理本企业项目，创建项目要求内置企业管理员。
- 写操作使用 PostgreSQL 事务级管理锁，取得锁后重新鉴权；数据库约束保护归属、唯一性、权限页面引用和版本。审计与业务写入同事务。
- 停用保留子级自身状态；移除企业成员清理项目成员和授权；恢复或重新加入不会恢复已撤销角色。
- 内置角色禁止编辑删除；新增目录权限自动补给内置管理员；菜单隐藏只影响导航，停用和祖先停用阻断对应接口权限。
- 受控部署证明、证明摘要、废弃清单、菜单子项和在用版本依赖核验；清理带事务、版本递增与 tombstone，重复操作幂等、种子不复活。
- `src/access-init.ts` 与 `access:init`：一次性指定已注册账号成为首位平台管理员，同账号重复执行安全，不会借重复初始化给其他账号提权。
- 账号状态参与每次会话校验；登录创建会话持用户行共享锁，禁用持排他锁。禁止停用最后一位平台管理员时不撤会话；审计失败时保持账号状态和会话。

## 2026-09-09 历史回归修复

1. Redis validate 脚本缺少局部 `isValidDigest` 导致所有 Bearer 接口 503：已补定义，并通过真实 Redis 有效会话与失效会话验证。
2. Redis revoke 脚本 `DO` 大写语法错误：改为 Lua `do`，真实账号禁用和测试清理通过。
3. 最后管理员停用被拒绝仍注销：会话撤销移到数据库业务校验、审计和结果读取之后，并保持用户排他行锁。
4. `UploadSessionParamsDto` 错用分片参数 schema：改为会话参数 schema，分片接口继续单独要求 `partNumber`。
5. 企业、项目、成员列表原先接受账号专属 `locked` 状态：拆分 `entityListSchema`，在请求边界拒绝非法枚举，避免 SQL 500。
6. 初始化脚本 ts-node 的 rootDir 问题：新增 `tsconfig.scripts.json` 并配置 `TS_NODE_PROJECT`；服务实际构建入口修正为 `dist/src/main.js`。

## 2026-09-09 历史验证结果

下列结果只覆盖当时实现与契约；其中的“通过”不证明 2026-09-17 新增的管理员选择、企业直接注册或项目成员候选流程已经通过。企业邀请协议尚未确认，不在当前复验范围。

| 检查                                                          | 结果                                                     |
| ------------------------------------------------------------- | -------------------------------------------------------- |
| 服务端当时全部测试，启用真实权限和队列集成                    | 6 个文件、33 项历史通过，0 跳过                          |
| 新增权限真实 PostgreSQL／Redis 集成                           | 14 项通过                                                |
| 新增权限策略／DTO 边界单测                                    | 5 项通过                                                 |
| 既有错误目录、队列服务、处理器及真实 Redis 队列回归           | 14 项通过                                                |
| 本轮文件定向 `vp check`                                       | 36 个文件格式通过；34 个文件无 lint 警告、错误或类型错误 |
| AC-12 追加测试定向 `vp check test/access.integration.spec.ts` | 1 个文件格式、lint 和类型检查通过                        |
| `vp exec tsc --noEmit`                                        | 通过                                                     |
| `vp run build`                                                | SWC 成功编译 89 个文件                                   |
| `vp run access:init` 缺少账号烟测                             | 输出明确用法并返回预期 exit 1，无 ts-node 配置异常       |

真实权限集成每次新建随机 `access_it_<uuid>` 数据库，执行迁移两次验证幂等，使用随机用户会话键；finally 清理数据库、会话和临时证明文件。队列测试采用自身随机 prefix。未执行全库 Redis 清理。

AC-12 追加后，由 QA 于 2026-09-09 23:19 使用下文相同命令复跑完整服务端测试，结果为 33/33 历史通过；后端随后完成新增测试文件的定向检查。此次仅增加测试证据，没有修改生产目录或产品逻辑。原测试中的跨企业项目成员约束不证明当前“从所属企业有效成员候选中按 `userId` 选择并排除已加入者”的完整流程已经通过。

14 项集成测试覆盖：

1. 一次初始化、同账号幂等、禁止其他账号提权、新账号无工作空间、初始化审计。
2. 企业／成员／项目创建、平台不自动入企业、企业管理员隐式项目权限、普通成员不能创建项目。
3. 项目创建审计失败时项目、角色、成员一起回滚。
4. 重复成员、内置角色、非法管理员专属授权、跨企业项目成员数据库约束。
5. 菜单隐藏保留权限、停用阻断管理员权限、受保护菜单不能停用。
6. 企业停用保留项目自身状态、移除成员清理项目授权、重新加入不复活角色。
7. 两种真实锁排队时序：业务写先持锁可提交；撤权先持锁时已入队业务写必须重新鉴权并拒绝。通过 `pg_locks` 确认等待状态，失败写没有成功审计。
8. 两位企业管理员并发停用自己，只能成功一次。
9. 账号禁用撤销真实 Redis 会话、平台补任替换、恢复不恢复旧管理员角色、最后平台管理员保护且会话保留。
10. 账号禁用审计失败时账号状态回滚且现有会话有效。
11. 登录共享行锁与禁用排他行锁真实协调，禁用完成后无残留登录会话。
12. 清理非废弃子项、仍启用功能、过期证明、变更证明、旧服务依赖、未选子项均阻断；审计失败全回滚、成功幂等、tombstone 和种子不复活。
13. 菜单乐观版本、数据库正版本约束、审计敏感字段检查。
14. AC-12 真实目录升级：先初始化并创建自定义角色、成员分配和菜单定制，再向测试内存目录加入新页面与操作，使用真实 `getBuiltinPermissionKeys` 同步。仅企业范围内置管理员的权限和角色版本扩展；平台／项目角色、内置普通成员、自定义角色权限和用户角色分配保持不变；既有菜单名称、父级、图标、排序、隐藏和停用等全部字段保持；仅对应范围菜单版本递增，重复同步完全幂等。

5 项策略／DTO 测试覆盖默认拒绝、明确公开和仅登录策略、公司／项目范围解析、列表状态枚举、上传会话与分片参数区分。

## 历史基线复现命令

以下命令用于复现 2026-09-09 测试基线，不自动覆盖 2026-09-17 待复验范围。临时配置由主控创建，仅存在本机临时目录，不写入仓库：

```powershell
$env:RUN_ACCESS_INTEGRATION='true'
$env:RUN_QUEUE_INTEGRATION='true'
node scripts/with-enterprise-test-env.mjs "$env:TEMP/codex-enterprise-01a085-runtime.json" apps/server C:/Users/Administrator/.vite-plus/bin/vp.exe test
```

在 `apps/server` 目录运行定向检查：

```powershell
vp check src/modules/access src/database/access.schema.ts src/database/schema.ts src/common/decorators/access-policy.decorator.ts src/modules/auth/auth.controller.ts src/modules/auth/auth.repository.ts src/modules/auth/auth.service.ts src/modules/auth/guards/session-auth.guard.ts src/modules/auth/session/session.script.ts src/modules/auth/session/session-store.service.ts src/modules/upload/upload.controller.ts src/modules/upload/dto/upload-params.dto.ts src/app.module.ts src/access-init.ts test/access.integration.spec.ts test/access-policy.spec.ts test/error-catalog.spec.ts package.json tsconfig.scripts.json
vp exec tsc --noEmit
vp run build
```

部署时先执行数据库增量迁移，构建 shared 和 server，再使用已实跑的 `node dist/src/access-init.js --account <已注册完整账号>` 初始化。源码方式为 `vp run access:init -- --account <已注册完整账号>`；运行入口为 `vp run build:preview`。已初始化的环境无需重新指定管理员。

## 范围与未测边界

- HTTP、浏览器和真实 MinIO 上传验收由 QA 负责，具体计数和截图以 QA 报告为准；本报告不将服务层测试描述成浏览器测试。
- 清理成功路径使用测试模块扩展的废弃页面／操作目录。生产首版没有虚构的废弃功能，因此生产界面预览为空属于预期。
- 管理写操作当前共用一把事务级 advisory lock，优先保证撤权与业务修改的顺序。本轮未进行大规模企业／项目的吞吐和性能压测。
- PostgreSQL 和 Redis 没有共同的分布式事务。本轮覆盖业务检查失败、审计失败、真实行锁与正常会话撤销；没有注入数据库最终 COMMIT 与 Redis 连接同时故障的组合场景。
- 全仓已有无关格式问题，由主控记录；本轮只修复和检查自身改动范围。

## 后端知识点

事务负责一组数据库写入共同提交或回滚；锁负责并发顺序。把鉴权放在取得写锁之后，才能防止排队请求使用已经撤销的权限。`expectedVersion` 则负责发现用户提交旧表单，两者解决不同问题。

会话位于 Redis，账号状态和权限位于 PostgreSQL。用户行共享／排他锁协调登录与禁用：登录创建会话期间不能被禁用插入，禁用完成后登录会重新检查账号状态。最后管理员和审计检查必须先完成，才能避免被拒绝的操作产生会话副作用。

## 变更记录

| 日期 | 变更摘要 | 影响范围 |
| --- | --- | --- |
| 2026-09-17 | 增加全局管理员选择、企业直接注册及项目企业成员候选的后端复验清单；企业邀请仅保留产品入口，具体协议待确认并排除当前 DTO、Schema、端点与后端复验 | DTO、Schema、迁移、控制器、事务、审计和集成测试 |
