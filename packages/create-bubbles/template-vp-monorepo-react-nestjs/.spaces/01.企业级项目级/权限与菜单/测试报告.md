# 企业、项目与菜单权限独立验收报告

> 文档状态：历史基线＋当前待复验。

历史验收日期：2026-09-09。该轮依据当时的 [PRD](./PRD.md) 和 [技术契约](./技术契约.md)，独立 QA 的 HTTP、浏览器、旧库迁移及旧库 HTTP 共 35 组全部通过，0 失败、0 跳过；15 项验收在下述本机与受控夹具范围内通过。新增目录升级专项已用真实 PostgreSQL 复验。以上结果只说明当时版本，不代表当前需求已经验收通过。

2026-09-17 成员接入规则已变更：平台创建企业、设置或更换企业管理员使用全局账号选择器并提交 `administratorUserId`；企业新增成员保留“直接注册新账号并加入企业”和“企业邀请”两条产品路径，但邀请协议尚未确认；项目成员只从所属企业有效成员中按 `userId` 选择，并排除已经加入当前项目的成员。下述 2026-09-09 结果作为历史基线保留，不能证明已确认的新入口已经通过；邀请不进入当前验收。

## 2026-09-17 变更复验状态

| 验收项 | 当前状态 | 必须补充的证据 |
| --- | --- | --- |
| AC-01 完整业务流程 | 待复验 | `GlobalAccountSelect` 开通企业及设置／更换管理员；企业直接注册；从所属企业有效成员候选中按 `userId` 添加项目成员。企业邀请待协议确认后另补证据 |
| AC-02 权限隔离 | 待复验 | 企业侧不能枚举全局账号；项目候选只返回所属企业有效且尚未加入当前项目的成员 |
| AC-06 管理约束 | 待复验 | 企业直接注册账号冲突与失败回滚；项目跨企业、停用和重复关系拒绝；企业邀请异常分支待协议确认后另补 |
| AC-07 数据与审计 | 待复验 | 直接注册和项目成员新增的事务与审计；密码及敏感字段不进入日志、响应与审计。邀请事务与审计规则待协议确认后另补 |

企业邀请目前只确认产品入口，具体契约尚未确认，不进入本轮增量验收。当前待复验范围为管理员选择、企业直接注册和项目成员候选；尚无本轮 Shared、Server、Web、真实 PostgreSQL 或浏览器执行结果，因此不能记为通过。

## 环境与证据来源

- 本机隔离 PostgreSQL 18：127.0.0.1:15432，主库 enterprise_test；Redis 8：127.0.0.1:16379；MinIO：127.0.0.1:19000，专属测试 bucket。未访问开发数据库或原对象存储。
- 主 HTTP 服务：http://127.0.0.1:3301；前端：http://127.0.0.1:5301，/api 代理到 3301。Chrome headless，1440×1000，中文环境。均连接真实后端，业务流程没有替换为 mock。
- 旧库验证新建随机 `enterprise_legacy_*` 库，重放原 0000/0001 后升级。旧账号 HTTP 验证临时启动当前构建服务于 3302，使用独立 Redis DB 2／3，验证后关闭该进程。
- 运行配置、测试账号、密码及会话仅保存于系统临时目录或进程内存。输出仅含用例 ID 和脱敏结果；截图遮挡完整测试账号，不保存含凭据的 trace 或 HAR。API 测试使用 mobile 会话槽、浏览器使用 web 槽，遵循现有同终端单会话规则。
- 独立 QA 直接执行并保存 [HTTP 17 组](./测试证据/http.json)、[浏览器 13 组](./测试证据/browser.json)、[旧库迁移 3 组](./测试证据/legacy.json)、[旧库 HTTP 2 组](./测试证据/legacy-http.json)。浏览器注册、登录、开通企业、创建项目、分配角色、管理员替换均通过真实 UI 操作。
- 后端提供的服务层、真实数据库／Redis 和策略测试由 QA 于 23:19 再次直接执行：6 文件、33/33 通过，0 跳过（14 项权限集成、5 项策略／DTO、14 项既有回归），包括新增目录升级。共享包于 23:20 直接执行、未使用任务缓存：7/7 通过。前端负责的 43 项回归和构建见 [前端交接](./前端交接.md)，后端断言说明见 [后端报告](./后端测试报告.md)。这些结果与 QA 的 35 组分别统计，不混算为 35 个独立单一断言。

## 验收台账

本台账记录 2026-09-09 的历史结果。“历史通过”只表示当时版本在下列本机和受控夹具范围内符合当时要求，不表示当前需求或生产环境发布验收已经通过。

| 项目                     | 状态                           | 实际验证与证据                                                                                                                                                                                                                                                                                                                                                               |
| ------------------------ | ------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| AC-01 完整业务流程       | 历史通过；新入口待复验         | HTTP 企业／项目创建及失败无残留；浏览器真实注册登录、普通无企业空态、无企业平台入口、开企业、按当时完整账号入口加企业成员、建项目、自定义角色授权、项目成员访问。旧完整账号添加证据已失效，不能覆盖直接注册或企业候选新增项目成员；企业邀请协议待确认，旧证据也不能证明邀请能力。                                                                                                                                                          |
| AC-02 权限隔离           | 历史通过；新规则待复验         | HTTP 跨企业／项目资源统一 404，平台身份不自动成为企业成员；企业管理员无显式项目成员记录仍可管理本企业项目。浏览器普通项目成员只有允许入口，越界资源显示无资源页。                                                                                                                                                                                                            |
| AC-03 菜单一致性         | 历史通过                       | HTTP 隐藏保留直接访问，停用及祖先停用阻断管理员；页面／操作权限共同校验。浏览器撤权后按钮消失；平台首页隐藏可直达，停用后可从其他授权导航进入菜单页并恢复。                                                                                                                                                                                                                  |
| AC-04 即时撤权           | 历史通过                       | HTTP 角色撤权、成员停用／移除、企业停用和账号禁用后重新请求立即拒绝；浏览器另一终端撤权后 focus 刷新显示 403。账号恢复后旧会话仍失效，需重新登录。                                                                                                                                                                                                                           |
| AC-05 切换与缓存         | 历史通过                       | Playwright 受控延迟旧企业 access 请求，切到项目再释放响应，目标 scope 与页面不被覆盖；退出换空账号无旧企业数据。前端回归另覆盖旧 401／403、请求取消及 Router AbortSignal。                                                                                                                                                                                                   |
| AC-06 管理约束           | 历史通过；新规则待复验         | HTTP 重复成员、非法项目成员、DTO 未知字段、分页上限、跨作用域角色、旧版本 409、内置角色不可改、最后公司／平台管理员保护、菜单循环／引用删除／受保护入口均拒绝。后端真实 PG 测试证明两管理员并发停用只成功一次。                                                                                                                                                              |
| AC-07 数据与审计         | 历史通过；新规则待复验         | HTTP 成功写审计与响应中的服务端 x-request-id 对应，失败请求没有成功审计，查询遵循范围边界。后端在项目创建中注入审计失败，项目、角色、成员一起回滚；另检查审计摘要无密码／token 字段。                                                                                                                                                                                        |
| AC-08 回归与迁移         | 历史通过                       | 空库迁移、正式初始化 CLI；旧库真实迁移保留用户 ID、密码散列与上传 ownerId，不自动加入企业；重复 migrator 不重复变更。升级后旧账号在新服务真实 HTTP 登录、读取自己的旧上传记录，跨 owner 404。真实 MinIO 4 字节分片上传与完成成功。前端既有 storage／request、后端既有错误目录／队列回归通过。                                                                                |
| AC-09 停用与恢复         | 历史通过                       | HTTP 企业／成员恢复保留子级原本的 disabled 状态；移除企业成员清理项目关系和授权，重加不恢复旧角色；禁用与启用账号均使旧会话失效。浏览器停用企业、替换管理员、保持停用、启用后新管理员可管理。                                                                                                                                                                                |
| AC-10 管理员补任与更换   | 历史通过；新选择入口待复验     | HTTP 平台禁用唯一企业管理员后可补任替换，旧账号恢复不重获已撤身份；最后平台管理员保护不被绕过。浏览器在停用企业内完成替换，先保持停用再启用，替换人具有内置企业管理员能力。后端真实事务验证账号状态／审计失败回滚。                                                                                                                                                          |
| AC-11 创建项目身份限制   | 历史通过                       | 企业内置管理员可创建；HTTP 普通成员、项目管理员和非法授权请求均不能下放或绕过。浏览器自定义角色权限树不含“创建项目”，全选后普通成员仍无创建按钮。                                                                                                                                                                                                                            |
| AC-12 内置角色与新增功能 | 历史通过                       | HTTP 人工修改内置角色拒绝；重复 CLI 初始化保留菜单配置、角色分配，同账号安全、另一账号不能提权。真实 PG 升级专项在已有 bootstrap、角色、用户分配和定制菜单之后注入新版企业页面／操作目录，运行种子：只扩企业内置管理员并递增其版本，其他范围管理员、member／自定义权限不变，用户分配与既有菜单的父级／显示／状态／排序保持，新增页面和操作正确绑定；再运行种子完整快照不变。 |
| AC-13 前端版本兼容       | 历史通过（受控响应夹具）       | Playwright 在浏览器响应中注入未知 routeKey 页面／空目录，验证过滤、未知 URL 404、服务端原始菜单不改变。前端菜单映射测试覆盖所属操作过滤、静态已注册页面按授权／菜单状态显示。没有实际部署两个前端版本。                                                                                                                                                                      |
| AC-14 废弃权限清理       | 历史通过（受控目录／部署证明夹具） | 首版无废弃项：HTTP 空预览、非法候选拒绝；浏览器显示空态且确认按钮 disabled。后端真实 DB 受控废弃目录与部署证明测试覆盖有效子项／活动功能／过期或变更证明／旧服务依赖／未选子项阻断，审计失败整体回滚、成功幂等、版本递增、tombstone 与种子不复活。没有在生产多实例环境执行清理。                                                                                             |
| AC-15 在途写操作与撤权   | 历史通过                       | QA 用真实 HTTP 请求及 PG advisory lock／pg_locks 排队确认：撤权先持锁时写请求等待，之后 403 且无成功审计；写先持锁时先成功提交，撤权随后完成，后续写被拒绝。后端独立真实 PG 集成再次覆盖两种顺序与锁内重新鉴权。                                                                                                                                                             |

## 本轮问题回流与复验

以下实际业务缺陷已回派负责方修复，并在 2026-09-09 历史基线中通过相关 HTTP 和浏览器复验：

| 问题                                                         | 修复及复验                                                                                               |
| ------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------- |
| Bearer 接口全部 503，Redis validate Lua 缺局部 isValidDigest | 后端补定义；真实登录后的全部 HTTP 组、真实 Redis 集成通过。                                              |
| Redis revoke Lua 使用大写 DO 导致脚本错误                    | 改为 Lua do；账号禁用、恢复及会话撤销真实验证通过。                                                      |
| 拒绝停用最后平台管理员却提前注销原会话                       | 会话撤销放到业务约束与审计之后；拒绝停用后原会话仍可请求，后端审计失败用例也保留会话。                   |
| 上传会话 DTO 错用必需 partNumber 的分片 schema               | 还原会话参数 schema；真实 MinIO 上传创建、分片、完成和归属校验通过。                                     |
| 首页停用后父布局失去导航，无法到菜单页恢复                   | 前端保留父级 scope 上下文，并选择可见授权页面落点；浏览器完成首页停用→其他入口→菜单恢复→首页的完整流程。 |
| 资料保存没有更新本地 version，第二次编辑误报 409             | 保存后合并服务端最新实体与 version；浏览器连续两次保存均 200，最终内容正确。                             |

QA 脚本另修正了定位与证据采集：AntD 图标参与按钮 accessible name；403／404 的数字是 SVG，按真实文案定位；审计采用响应请求 ID；API 与浏览器终端槽分离；截图等待页面就绪及弹窗稳定并禁用采集时动画。以上属于测试脚本修正，不列为产品缺陷。fetch 仅在非 GET／HEAD 且有内容时附请求体，未屏蔽 lint 规则。

## 截图证据

2026-09-09 历史验收的 9 张实际截图保存在 `ui/`，已解码并逐张视觉检查。页面没有空白或加载占位；完整测试账号已遮挡。紫色区域为 QA 凭据遮罩。这些截图不证明 2026-09-17 变更后的成员入口已经实现或通过。

| 文件                                                                            | 内容                                           |
| ------------------------------------------------------------------------------- | ---------------------------------------------- |
| [workspace-empty.png](./ui/workspace-empty.png)                     | 普通新账号等待企业管理员添加的空态             |
| [company-created.png](./ui/company-created.png)                     | 平台真实开通企业后的列表                       |
| [project-created.png](./ui/project-created.png)                     | 企业管理员真实创建项目                         |
| [member-role-assigned.png](./ui/member-role-assigned.png)           | 成员列表中已分配的自定义角色                   |
| [project-member-home.png](./ui/project-member-home.png)             | 普通项目成员的工作台与有限导航                 |
| [permission-revoked.png](./ui/permission-revoked.png)               | 撤权后 403 页面与剩余可用导航                  |
| [platform-menu-recovery.png](./ui/platform-menu-recovery.png)       | 平台首页停用并恢复后的完整工作台               |
| [cleanup-empty-preview.png](./ui/cleanup-empty-preview.png)         | 废弃权限为空、确认清理按钮禁用                 |
| [replacement-administrator.png](./ui/replacement-administrator.png) | 替换管理员登录恢复后的企业，并具有创建项目入口 |

## 检查与复现

已执行 `vp check scripts/enterprise-qa .spaces/01.企业级项目级/权限与菜单/测试报告.md`：7 个文件格式正确，6 个脚本无 lint 警告、错误或类型错误；限定范围 `git diff --check` 通过。全仓原有 176 处格式问题，以及 `packages/i18n/core/src/i18n.ts` 的两处 console 类型错误、3 项警告由主控记录，本轮未批量改写无关文件；不把原有全仓失败描述为本轮全部检查通过。

主控先按 [运行说明](./运行说明.md) 准备独立运行环境、构建当前 shared／server、启动 3301／5301。配置绝对路径保存在系统临时目录，由下列命令作为参数传入，不将其内容粘贴到命令或仓库：

验收结束后，主控已于 2026-09-10 清理本次专属服务、容器、匿名卷和临时凭据；重新执行这些命令前需重新准备隔离环境。测试证据仍保留于本目录。

```powershell
$runtimePath = Join-Path ([System.IO.Path]::GetTempPath()) 'codex-enterprise-01a085-runtime.json'
node scripts/enterprise-qa/legacy.mjs $runtimePath prepare
node scripts/enterprise-qa/legacy.mjs $runtimePath verify
node scripts/enterprise-qa/legacy-http.mjs $runtimePath
node scripts/enterprise-qa/http.mjs $runtimePath
node scripts/enterprise-qa/browser.mjs $runtimePath
vp check scripts/enterprise-qa .spaces/01.企业级项目级/权限与菜单/测试报告.md
```

http.mjs 使用随机注册账号并调用正式首管理员初始化 CLI，保存供浏览器使用的私有状态；因此 browser.mjs 必须在其后运行。首次运行应使用独立新环境，重复测试不能借初始化更换平台首位管理员。legacy-http.mjs 只在 3302 空闲时启动自己的测试进程并在结束时关闭。主环境清理由主控统一执行，不清空其他 Redis、数据库或存储。

浏览器脚本无需安装新依赖。默认使用本机 Codex runtime 中的 Playwright 和已安装 Chrome，可通过 ENTERPRISE_QA_PLAYWRIGHT_ROOT 指定含 package.json 的运行时根目录、ENTERPRISE_QA_CHROME_PATH 指定 Chrome 可执行文件。默认路径只是本机 QA 工具位置，不属于产品运行要求。

## 验证边界

- 本轮覆盖真实功能、数据库事务、锁时序、会话撤销、对象存储和浏览器流程；未做大规模吞吐压测、移动端／多浏览器兼容矩阵或生产发布。
- AC-13 使用未知前端路由响应夹具与静态注册映射测试，AC-14 使用受控废弃目录与部署证明，不声称已实际完成多版本前端部署或生产多实例滚动发布。
- 旧上传夹具验证历史数据库记录及 owner 归属，不声称夹具的旧对象已有真实二进制内容。真实 MinIO 写入由主 HTTP 上传回归另外验证。
- PostgreSQL 与 Redis 没有分布式原子事务。已验证业务检查失败、审计失败、真实用户行锁及正常撤销；未注入最终 COMMIT 与 Redis 同时故障的组合场景。
- 未执行、环境阻塞、跳过与仅静态检查均不计为真实 HTTP／浏览器通过；报告中不同层级的证据已分别标明。

## 变更记录

| 日期 | 变更摘要 | 影响范围 |
| --- | --- | --- |
| 2026-09-17 | 标记成员接入新规则的增量复验边界；管理员选择、企业直接注册和项目成员候选进入当前验收，企业邀请仅保留产品入口、具体协议待确认并排除当前验收 | AC-01、AC-02、AC-06、AC-07、管理员选择、企业直接注册、企业邀请与项目成员候选 |
