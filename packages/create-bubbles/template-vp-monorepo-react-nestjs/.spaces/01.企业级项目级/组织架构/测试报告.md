# 企业／项目组织架构、岗位与成员选择器测试报告

状态：独立企业方案 QA 待复验；企业／项目内部组织、岗位和选择器的既有证据保留，真实浏览器 E2E 仍受 Playwright 环境限制。日期：2026-09-17。

本报告依据当前 [需求文档](./需求文档.md)、[技术契约](./技术契约.md) 和 [联调记录](./联调记录.md)，逐条核对 AC-01～AC-36。2026-09-17 取消企业间父子关系后，旧企业层级方案的实现、迁移和测试证据已经失效；在独立企业边界完成实现同步并复验前，不能沿用旧报告的整体验收结论。

## 1. 当前结论

| 状态 | 数量 | 验收项 |
| --- | ---: | --- |
| 通过 | 22 | AC-01～AC-02、AC-07～AC-11、AC-13～AC-24、AC-33～AC-35 |
| 失败 | 0 | 无 |
| 待实现同步与复验 | 6 | AC-03～AC-06、AC-31、AC-36 |
| 无法完整验证（部分证据通过） | 8 | AC-12、AC-25～AC-30、AC-32 |
| 合计 | 36 | AC-01～AC-36 |

当前结论不是“整体通过”：

- AC-03～AC-06 和 AC-36 必须在移除企业父级、企业类型、企业排序、企业树查询／移动及相关权限后重新验证。
- AC-31 的成员入口语义已经变更：企业成员仅允许直接注册或邀请，邀请细节待确认；项目成员仅从所属企业有效成员中按 `userId` 选择并排除已加入者。旧“完整账号添加成员”证据失效，需求、契约和实现同步前不能记为部分通过。
- AC-25 的模板创建事务证据仍可参考，但旧版 `0003` 迁移证据属于已取消方案；当前待交付的 `0003` 尚未应用到当前数据库，也未按独立企业方案重跑。
- AC-12、AC-26～AC-30 和 AC-32 仍缺真实浏览器关键路径验收；AC-31 的需求与契约已确认，但仍须完成实现同步和增量复验。
- 企业／项目内部组织树、岗位、成员关系、模板和选择器的既有证据不等于独立企业边界已经验证通过。

## 2. 证据边界

### 2.1 2026-09-16 已执行记录

以下命令在取消企业间父子关系前执行。结果可作为未受本次产品变更影响能力的回归参考，不能证明独立企业边界已经落地；实现同步后必须重新执行相关测试与构建。

| 层级 | 历史命令 | 历史结果 | 当前适用范围 |
| --- | --- | --- | --- |
| Shared 测试 | `pnpm --filter shared test` | 2 个文件，14/14 通过 | 组织 DTO、规范化和候选能力可参考；企业禁用字段需复验 |
| Shared 构建 | `pnpm --filter shared build` | 成功，9 个产物文件 | 实现同步后复跑 |
| Server 普通测试 | `pnpm --filter server test` | 26/26 个已运行用例通过，20 个环境门控跳过 | 组织领域单测可参考；独立企业与迁移需复验 |
| Server 类型检查 | `pnpm exec tsc --noEmit -p apps/server/tsconfig.json` | 通过 | 实现同步后复跑 |
| Server 构建 | `pnpm --filter server build` | 成功，127 个文件编译完成 | 实现同步后复跑 |
| Web 全量测试 | `pnpm --filter web test` | 14 个文件、106/106 通过 | 组织、岗位、选择器和工作空间隔离证据继续保留 |
| Web 类型检查与构建 | `pnpm --filter web build` | 通过 | Web 代码变化后复跑 |
| Web 测试类型检查 | `pnpm exec tsc --noEmit -p apps/web/test/tsconfig.json` | 通过 | Web 测试变化后复跑 |
| Playwright 可用性 | `pnpm exec playwright --version` | 命令不存在 | 真实浏览器流程未验证 |

Server 的 20 个跳过项包括 19 个访问／组织真实集成用例和 1 个队列真实集成用例；跳过项没有计入 26 个通过项。

### 2.2 历史真实环境证据

同一开发会话此前在隔离 PostgreSQL／Redis 中设置 `RUN_ACCESS_INTEGRATION=true` 并运行 `apps/server/test/access/access.integration.spec.ts`，历史结果为 19/19 通过。该测试创建随机数据库、连续执行两次 migrate，并在结束后删除测试数据库。

该结果运行于旧企业层级方案：

- 组织单元、岗位、模板、成员关系、项目创建回滚和选择器候选相关断言可作为回归参考。
- 与 `entityType`、企业父子关系、企业移动、企业同父约束、企业层级权限或旧版 `0003` 结构有关的断言全部失效。
- 跨企业隔离和项目直接归属虽然有历史证据，但实现边界已经调整，AC-05、AC-06 仍须在独立企业代码与迁移上重跑。

本次文档同步没有 PostgreSQL／Redis 连接参数，也没有重新执行真实集成测试。

### 2.3 当前契约目标与尚待复验项

| 项目 | 当前契约目标 | 当前 QA 状态 |
| --- | ---: | --- |
| 实际受影响端点 | 现行组织契约 45 | 41 个契约表端点 + 4 个既有行为变更端点，待复验 |
| 新增权限 key | 21 | 旧版 22 个统计失效，待移除企业层级权限后复核 |
| 新增领域错误码 | 7 | 错误码集合未因独立企业变更调整，仍需随实现复跑 |
| 新增组织相关表 | 10 | 表清单沿用，待最终迁移验证 |
| 新增字段 | 97 | `companies` 应为零新增字段；旧版 101 个统计失效 |
| 数据库 `0003` | 尚未应用 | 必须先清理企业层级 DDL／预检／索引／约束并完成隔离环境验证 |
| 成员入口 | 需求待同步完整，随后实现与复验 | 企业为直接注册或邀请，邀请细节待确认；项目从所属企业有效成员按 `userId` 选择并排除已加入者 |

`AccessScope` 仍只有 `platform | company | project`；组织、岗位和模板稳定资源继续不提供物理删除接口。这些结构原则没有因独立企业决策改变。

### 2.4 Web 自动化证据

前端第 6 轮修复后的 Web 历史全量结果为 14 个文件、106/106 通过。以下专项测试仍能证明不依赖企业间层级的请求所有者绑定、导航边界、选择器回显、跨页选择、关系禁选和最新请求协调逻辑：

- `apps/web/test/workspace-requests.spec.ts`
- `apps/web/test/workspace-navigation-boundary.spec.tsx`
- `apps/web/test/router/entry.spec.ts`
- `apps/web/test/router/workspace-data.spec.ts`
- `apps/web/test/selector/global-account-select.spec.ts`
- `apps/web/test/selector/selector-selection.spec.ts`
- `apps/web/test/selector/organization-member-selector.spec.ts`
- `apps/web/test/organization-assignment.spec.ts`
- `apps/web/test/latest-dialog-request.spec.ts`

这些自动化覆盖 A→B→C 工作空间切换、旧请求隔离、导航卸载、权限重校验时的 `hidden + inert`、静态 Modal 清理、选择器快照补查和迟到请求保护。它们不能替代真实浏览器中的 Router pending、Ant Design portal、原生 `inert`、焦点／鼠标／键盘和逐角色权限验收。

### 2.5 明确失效的旧证据

以下内容不得再标记为当前方案“通过”：

- `CompanyRecord` 或请求中存在 `parentCompanyId`、`entityType`、企业层级 `sort` 等字段。
- 平台企业树、父级选择、企业移动、企业循环保护、企业同父名称／排序及相关权限、审计或页面。
- 把集团作为企业类型表达的旧结论。
- Drizzle 快照中 `companies` 新增 4 个字段以及 101 个总字段变更。
- 将旧版 47 个受影响端点继续当作现行目标，或把未经确认的邀请端点计入组织专项统计。
- 旧版 `0003` 在随机测试库通过即可代表当前迁移已验证的结论。
- 企业或项目成员继续按完整账号直接添加的旧接口、页面与测试结论。

## 3. AC-01～AC-36 验收矩阵

状态说明：

- “通过”表示现有证据仍与当前独立企业方案中的该项行为匹配。
- “待实现同步与复验”表示需求与契约已经确认，但实现、迁移、增量测试或禁用概念扫描尚未按新方案形成完整证据。
- “无法完整验证（部分证据通过）”表示已有单元、接口或静态证据，但仍缺迁移、真实浏览器或前后端一致性验收。

| AC | 状态 | 主要证据与结论 |
| --- | --- | --- |
| AC-01 全局人员标识 | 通过 | 历史真实集成测试覆盖同名账号按不同 `userId` 区分；Shared 与选择器持久值使用 `userId`。 |
| AC-02 作用域保持 | 通过 | Shared 与既有实现均只使用 platform、company、project；组织单元和岗位没有形成第四种工作空间。 |
| AC-03 独立企业模型 | 待实现同步与复验 | 目标是 `companies` 不新增企业父级、类型、排序或层级规范键。旧 Schema／DTO 证据与该目标冲突，必须清理后做快照和全仓扫描。 |
| AC-04 无企业间树能力 | 待实现同步与复验 | 旧企业树、父级选择、企业移动和循环保护证据全部失效；需确认路由、权限、页面、审计和测试中均无此能力。 |
| AC-05 跨企业隔离 | 待实现同步与复验 | 历史隔离测试可作参考，但需在无企业父子模型的 Repository、权限和候选查询上重新执行跨企业 404／无数据泄露测试。 |
| AC-06 项目直接归属 | 待实现同步与复验 | 项目直接 `companyId` 的历史证据仍可参考；企业层级代码清理后需确认项目归属、模板和组织校验没有残余父企业／兄弟企业语义。 |
| AC-07 企业项目分树 | 通过 | 组织树按企业／项目作用域独立建模，领域测试覆盖路径、深度、有效状态与 `hasChildren`；这里的树只指作用域内部组织树。 |
| AC-08 跨树写入拒绝 | 通过 | 组织 Repository 以组织树和作用域约束资源，历史真实集成测试覆盖跨企业组织资源隔离。 |
| AC-09 组织命名、深度与路径 | 通过 | Shared 测试覆盖 NFKC；领域测试覆盖 10 层、缺父节点和组织单元循环；同树编码及同一组织父单元下名称唯一。 |
| AC-10 整数排序 | 通过 | strict schema 只接受范围内整数；组织单元按 `sort ASC, id ASC` 返回，数据库只持久化单一整数 `sort`。 |
| AC-11 默认排序 | 通过 | 领域测试覆盖首个同级组织单元为 1、后续最大值加一及排序上限耗尽。 |
| AC-12 移动排序 | 无法完整验证（部分证据通过） | 服务和领域测试覆盖组织单元同树移动、路径与深度重算；尚未在真实浏览器验证缓存中的节点及后代均及时刷新。 |
| AC-13 多组织关系 | 通过 | 企业与项目成员关系表彼此独立，历史真实集成测试确认同一成员可加入多个本作用域组织单元。 |
| AC-14 无主归属 | 通过 | DTO、数据库、接口和 Web 证据均无主归属／默认组织字段，关系语义不依赖数组顺序。 |
| AC-15 关系唯一与默认顺序 | 通过 | 复合唯一约束、事务替换、作用域校验和关系排序已有 Shared、服务及历史集成证据；无 `memberSort`。 |
| AC-16 主要负责人唯一 | 通过 | 历史真实集成测试覆盖 leader 原子交接、双 leader 整批失败及同一人负责多个组织单元。 |
| AC-17 负责人职责与资格 | 通过 | Shared 枚举和服务校验覆盖 member、leader、deputy；组织职责不写 RBAC 角色。 |
| AC-18 未归属视图 | 通过 | 服务按“有效成员且无组织关系”查询虚拟分组，数据库没有未归属实体记录。 |
| AC-19 岗位完全独立 | 通过 | strict DTO 拒绝岗位与组织单元／组织树／模板节点关联字段，相关表也不持久化这些关联。 |
| AC-20 多岗位与唯一规则 | 通过 | 岗位任职复合唯一约束、集合替换、code 规范化和岗位候选作用域已有自动化或历史真实环境证据。 |
| AC-21 组织岗位解耦 | 通过 | 组织关系与岗位任职使用独立表、端点和服务，除成员清理外不存在互相联动。 |
| AC-22 模板初始化方式 | 通过 | Shared 判别联合、模板树领域校验及历史真实集成测试覆盖版本、状态与直接所属企业约束。 |
| AC-23 模板复制边界 | 通过 | 领域测试覆盖模板组织节点父子 ID 重映射；历史集成测试确认只复制组织与独立岗位，不复制成员、负责人、任职或角色。 |
| AC-24 项目独立演进 | 通过 | 历史真实集成测试确认项目保存模板快照，来源模板后续变化不回写项目组织或岗位。 |
| AC-25 初始化与存量迁移事务 | 无法完整验证（部分证据通过） | 项目创建审计失败整体回滚和模板复制事务有历史证据；旧版 `0003` 迁移证据失效，当前迁移未应用且尚未按独立企业方案完成空库、重复执行、存量 backfill 与 rollback 验证。 |
| AC-26 页面作用域 | 无法完整验证（部分证据通过） | 自动化覆盖请求 owner key、A→B→C、导航卸载、重校验状态保留及 `hidden + inert`；尚缺真实浏览器全过程。 |
| AC-27 组织成员候选范围 | 无法完整验证（部分证据通过） | 服务、领域测试和历史集成测试覆盖作用域与 purpose 裁剪；尚缺真实浏览器筛选组合及展示验收。 |
| AC-28 弹窗搜索与去重 | 无法完整验证（部分证据通过） | 服务端分页、按 `userId` 去重聚合及前端跨页／最新请求测试已有证据；仍待浏览器交互验收。 |
| AC-29 同名信息展示 | 无法完整验证（部分证据通过） | 响应包含完整账号、来源和组织完整路径，历史集成测试覆盖同名账号；仍待真实渲染、溢出和筛选验收。 |
| AC-30 禁选状态 | 无法完整验证（部分证据通过） | Shared／Server 校验和前端关系测试覆盖停用、越权与无效 ID；尚缺浏览器禁选原因、清空流程和前后端一致性验收。 |
| AC-31 账号新增与成员入口边界 | 待需求／实现同步与复验 | GlobalAccountSelect 仍只服务平台授权场景。企业成员改为直接注册或邀请，邀请细节待确认；项目成员只能从所属企业有效成员中按 `userId` 选择并排除已加入者。旧完整账号添加用例失效，相关需求、候选、保存校验和 UI 均需复验。 |
| AC-32 权限能力拆分 | 无法完整验证（部分证据通过） | Controller 策略和 purpose 最小权限已有历史证据；旧版 22 个 key 统计失效，需按 21 个当前 key 复核并完成逐角色浏览器验收。 |
| AC-33 资源归属保护 | 通过 | 组织、岗位、模板和关系 Repository 以公司／项目归属约束资源，跨作用域与不存在资源统一按 404 处理。 |
| AC-34 生命周期与清理 | 通过 | 组织、岗位、模板无 DELETE；历史真实集成测试覆盖成员移除清理且不影响其他企业的独立关系。 |
| AC-35 审计一致性 | 通过 | 组织、岗位、模板和关系写入与成功审计同事务，历史测试覆盖项目创建审计失败回滚；日志未引入密码或 token。 |
| AC-36 排除范围验证 | 待实现同步与复验 | 项目小组、主归属、岗位－组织关联和工作流仍有旧扫描证据；企业间父子关系与旧完整账号成员入口都必须在代码和迁移清理完成后重新全仓扫描，当前不能记为通过。 |

## 4. 浏览器验收缺口

真实浏览器验收不是当前唯一阻塞项；必须先完成独立企业实现与迁移同步。此后若准备 Playwright 环境，至少覆盖：

1. 企业／项目工作空间入口、A→B→C 快速切换和同路由权限重校验，确认旧树、筛选、分页、选择和迟到请求均不污染新作用域。
2. 组织单元创建、同树移动、重命名、停用／恢复后，当前节点和已加载后代的 fullPath、depth、effective 与 hasChildren 正确刷新。
3. 组织成员选择器的服务端分页、姓名／完整账号搜索、同名账号、同名组织完整路径、标签独立、去重和禁选原因。
4. GlobalAccountSelect 只在平台授权场景出现；企业成员页面提供直接注册入口，邀请能力在规则确认后另行验收；项目成员选择只返回所属企业有效成员、按 `userId` 提交并排除已加入者。
5. 仅查看、可分配、可维护等典型角色下，页面按钮、筛选能力和服务端 403／404 一致。
6. 同一页面连续打开 A→B 弹窗时，A 的迟到成功、失败和 `finally` 均不能覆盖 B 的内容或 loading。

## 5. 发布前必须完成

- 清理企业层级 Shared 类型、Schema、迁移、服务端、前端、权限、审计和测试后，重新执行禁用概念全仓扫描。
- 将企业成员直接注册、项目成员企业候选与已加入排除规则同步到 Shared、Server、Web 和测试；企业邀请待规则确认后再补契约和验收。
- 按当前契约复核 45 个组织专项受影响端点、21 个新增权限 key、7 个组织领域错误码、10 张新表、97 个新增字段，以及 `companies` 零新增层级字段。
- 修订 `0003` 后在隔离环境完成空库、连续 migrate、存量 backfill 与受保护 rollback；验证完成前不向当前数据库执行该迁移。
- 重跑 Shared、Server、Web 测试与构建，并在真实 PostgreSQL／Redis 中复验 AC-03～AC-06、AC-25、AC-31、AC-32 和 AC-36。
- 由用户选择准备 Playwright 环境继续 E2E，或明确接受现有自动化作为浏览器项的替代证据；未补齐前保留对应“无法完整验证”状态。
- 收口时执行变更文件定向检查、`git diff --check` 与必要的 staged 检查，不得复用旧企业层级通过结论。
