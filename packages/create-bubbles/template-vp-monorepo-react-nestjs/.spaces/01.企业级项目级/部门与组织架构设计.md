# 企业主体、组织单元、岗位与成员选择器设计

> 产品方案：已确认
>
> 技术契约：已确认，进入实施与联调阶段
>
> 日期：2026-09-16

本文只保留当前已确认产品方案的精简说明。实施时以 [PRD](../../artifacts/dev-team/organization/PRD.md) 和 [技术契约](../../artifacts/dev-team/organization/contract.md) 为单一真相源；本文不重复端点、字段和数据库约束。

## 一、核心结论

1. 集团和独立企业都是具有独立 `companyId` 的企业主体，通过企业父子关系形成层级，不新增集团作用域。
2. 每个企业有一棵企业组织树，每个项目有一棵项目组织树。界面可以聚合浏览，数据库不建立跨树父子关系。
3. 树节点统一称为“组织单元”。部门、事业部、中心、科室和班组只是客户对组织单元的不同命名和用法。
4. 企业组织主要表达企业治理、共享职能和人员归属；项目组织主要表达项目执行结构。两者分别维护，避免项目差异污染企业标准。
5. 一个成员可以加入当前作用域的多个组织单元，也可以担任多个岗位；所有组织关系地位相同，不设置默认或主要归属。
6. 岗位保留，但岗位是企业／项目作用域内的平面列表，与组织单元完全独立。
7. 不建设项目小组。稳定协作结构使用项目组织单元，临时协作由具体业务选择成员。
8. 组织单元排序只使用一个非负整数 `sort`，不拆分输入，不使用小数。
9. 平台账号以全局 `userId` 唯一标识；姓名可以重复，界面必须结合完整账号和来源作用域帮助识别。

## 二、企业主体与组织树的边界

### 2.1 企业主体层级

`companies` 使用可空的 `parentCompanyId` 自关联，并用 `entityType = group | company` 区分展示语义。

- 集团、子企业和独立企业继续使用 `company` 作用域。
- 每个企业主体拥有独立成员、管理员、角色、数据、审计和直接项目。
- 企业父子关系只用于层级和导航，不继承成员、权限或数据。
- 项目始终只属于一个直接企业；调整企业上级不会改变项目的直接 `companyId`。
- 企业层级禁止自身／后代循环，跨企业资源继续按不可见处理。

需要独立成员目录、管理员、RBAC、数据和项目归属的单位，应建成企业主体；只用于内部层级、汇报或分类的节点，应建成组织单元。

### 2.2 企业组织树与项目组织树

企业和项目分别拥有独立组织树：

```text
企业 A
├─ 企业组织树
│  ├─ 财务中心
│  └─ 人力资源部
└─ 项目甲
   └─ 项目组织树
      ├─ 工程部
      └─ 商务部
```

- 企业组织单元的父节点只能来自同一企业树。
- 项目组织单元的父节点只能来自同一项目树。
- 企业与项目本身是界面虚拟根，不创建持久化根节点。
- 组织单元只能在本树内移动，移动时保持 ID 不变。
- 每棵树最多 10 层，顶级真实组织单元计为第 1 层。
- 树按父节点懒加载，每次返回一个层级、完整路径和 `hasChildren`。
- 节点的完整路径和有效状态会受祖先变化影响；移动、重命名或状态变更后，前端必须刷新受影响节点及全部后代，无法可靠追踪时直接清空当前作用域组织树缓存。
- 同树编码唯一，同父名称唯一；唯一比较使用 `lower(NFKC(trim(value)))`。
- 编码接受 ASCII 大小写输入，新建或更新时统一保存并返回小写；名称只去除首尾空白并保留展示大小写。

## 三、成员、负责人和岗位

### 3.1 组织成员关系

- 企业组织关系只能绑定当前企业成员，项目组织关系只能绑定当前项目成员。
- 同一成员可加入多个本作用域组织单元，但同一成员与同一组织单元只有一条关系。
- 成员可以不属于任何组织单元；“未归属组织”只是查询和界面虚拟分组。
- 每条关系的职责固定为 `member | leader | deputy`。
- 每个组织单元最多一名 `leader`，可以有多名 `deputy`。
- 同一人可以负责多个组织单元；负责人身份不会自动授予管理员权限。
- 既支持按成员替换其全部组织关系，也支持在组织详情中按组织单元替换全部成员；后者只修改当前单元，不覆盖成员在其他单元的关系。两种方式均原子保存，空数组分别表示解除该成员全部组织关系或清空当前单元。
- 组织成员默认按 `leader → deputy → member`，再按关系 ID 稳定返回，不提供人工成员排序。

### 3.2 岗位

岗位用于表达董事、财务负责人、项目经理、资料员等稳定业务任职。

- 企业岗位只属于一个企业，项目岗位只属于一个项目。
- 岗位是平面列表，不设置父级，不进入组织树。
- 岗位、任职关系和模板岗位均不保存组织单元字段。
- 一人可以多岗，一岗可以多人；同一成员不能重复担任同一岗位。
- 任职既支持按成员批量维护，也支持在岗位详情中按岗位批量维护成员；按岗位保存不覆盖成员的其他岗位，空数组可清空当前岗位任职。
- 组织归属、岗位任职和 RBAC 角色分别维护、分别审计，任一方变化不会自动修改另外两方。

## 四、排序与状态

### 4.1 单整数排序

- 企业同级和组织单元同级均使用 `0～999999999` 的整数 `sort`。
- 前端只显示一个整数输入框，编辑时直接回显和提交原值。
- 同级统一按 `sort ASC, id ASC` 返回，相同 `sort` 合法。
- 首个同级节点默认 `sort = 1`，其余默认使用当前同级最大值加一。
- 达到上限时停止自动递增，要求用户先调整现有顺序。
- 移动时可显式填写新 `sort`；省略时排到目标父级最后。

### 4.2 停用与关系处理

组织单元、岗位和模板首期只支持停用／恢复，不提供物理删除。

- 停用组织单元保留节点、下级节点、成员关系和历史引用。
- 停用岗位保留岗位及任职关系。
- 停用成员保留组织关系和岗位任职，恢复后按当前关系重新可见。
- 全量替换时，既有停用关系可以原样保留或解除；按成员保存空数组表示解除该成员在当前作用域内的全部对应关系，按组织单元或岗位保存空数组表示清空当前资源的全部成员关系。
- 停用成员不能新增组织关系或岗位任职，也不能升级组织职责；既有职责只允许保留、移除或按 `leader → deputy → member` 降级。
- 停用或受无效祖先影响的组织单元不能新增成员或升级职责，停用岗位不能新增任职。

## 五、项目组织模板

企业通过项目组织模板提供通用标准，项目创建后仍可独立调整。

- 创建项目必须显式选择 `blank` 或 `template`。
- `blank` 不携带模板信息；`template` 必须携带模板 ID 和精确版本。
- 模板只可用于其直接企业下的项目，且必须处于有效状态。
- 模板分别保存组织结构和岗位定义，两者不建立关联。
- 初始化复制组织层级、名称、编码、说明、排序和岗位定义，并为项目生成新 ID。
- 模板组织单元不单独设置状态，存在的节点初始化为 active；模板岗位保留状态，disabled 岗位按 disabled 复制并由项目后续独立启用。
- 不复制真实成员、负责人、组织关系、岗位任职或角色。
- 模板的组织单元／岗位使用完整子项版本快照；项目只读取明确的 `templateId + templateVersion`。模板名称、说明、状态和默认标记只保存当前值，历史变化由审计记录，不承诺从模板表重建历史主表状态。
- 每个企业最多一个有效默认模板。设置新默认模板时，旧默认模板也生成保留完整子项的下一版本、递增版本并写审计。
- 模板后续修改、停用或恢复不会自动同步既有项目。

## 六、账号与成员选择器

### 6.1 全局账号选择

`GlobalAccountSelect` 只用于有权搜索平台账号的管理场景：

- 稳定值为 `userId`，不以姓名或账号作为关系主键。
- 支持远程搜索和按 `userId` 回显。
- 同名账号显示完整账号和状态。
- 普通企业或项目操作者不能借此枚举平台账号。
- 企业侧新增普通成员继续使用完整账号精确添加。

### 6.2 组织成员选择

`OrganizationMemberSelector` 只选择调用者当前可见的既有企业／项目成员：

- 表单中表现为 Select 风格字段，点击后打开弹窗式组织浏览器。
- 左侧按可见作用域、组织、岗位或角色筛选，右侧分页展示人员和已选项。
- 企业工作空间可在权限允许时查看本企业及其可见项目；项目工作空间只查看当前项目。
- 企业端读取项目身份先要求 `company.projects.read`，项目组织、岗位、角色标签和相应筛选再逐项目要求 `project.organization.read`、`project.positions.read`、`project.roles.read`，不能用企业项目查看权限替代。
- 搜索支持姓名或完整账号，结果按全局 `userId` 去重并聚合可见身份。
- 选择器保存值始终只有 `userId`；企业／项目作用域由调用和保存动作的 URL 决定，项目身份只用于展示、筛选和辨识。
- 组织标签显示完整路径，组织和岗位标签分栏，不暗示两者有关联。
- 组织成员维护返回关系 ID 和职责；单组织成员视图按职责和关系 ID 稳定分页。
- 岗位分配使用独立 purpose，只要求岗位查看与分配权限，不隐式要求组织查看权限。
- 停用候选显示禁选原因；既有停用关系可稳定回显和解除，但不能作为新增关系提交。

组件目录继续按功能拆分：`BasicTableSelector`、`GlobalAccountSelect`、`OrganizationMemberSelector` 各自独立，`Selector/src/Selector.tsx` 只负责统一导出，不做庞大的 mode 组件。

## 七、页面与权限

企业和项目分别从现有工作空间进入“人员与组织”，页面内部不再提供第二套企业／项目切换器。

页面包含三个并列入口：

1. 组织架构：懒加载组织树、全部成员、未归属成员、组织详情、负责人和成员关系。
2. 岗位：平面岗位列表、状态和任职人数。
3. 成员：当前作用域成员的全部组织和岗位标签。

权限继续依附现有 `platform | company | project` 三种 `AccessScope`：

- 查看、创建、更新、移动、成员分配、岗位维护、岗位分配和模板维护分别授权。
- 加入组织、担任岗位或成为负责人均不会自动获得 RBAC 权限。
- 后端按登录状态、作用域成员资格、功能权限和资源归属逐层校验。
- 跨企业、跨项目、跨组织树和不存在资源统一返回 404；当前作用域内权限不足返回 403。
- 所有写操作与成功审计保持同一事务，失败不得留下部分数据或成功审计。

## 八、实施基线

技术契约当前固定以下规模：

- 43 个契约表实际端点，其中 39 个新增、4 个现有端点调整请求 DTO；另有 4 个既有端点发生规范唯一或成员清理行为变化，合计 47 个实际受影响端点。
- 10 张新表，`companies` 增加 4 个字段，共 101 个字段变更。
- 22 个新增权限 key。
- 7 个新增领域错误码。

管理员选择统一使用 `administratorUserId`，不保留 `administratorAccount` 双字段兼容期。写事务使用 READ COMMITTED 与全局管理锁，聚合只读使用 REPEATABLE READ + READ ONLY。

准确的 DTO、端点、数据库约束、事务、错误码和迁移顺序全部以 [技术契约](../../artifacts/dev-team/organization/contract.md) 为准。

## 九、本期明确不做

- 项目小组或第四种访问作用域。
- 默认组织、主要归属或按数组顺序推导的特殊组织关系。
- 岗位与组织单元的持久关联。
- 成员人工排序和双框排序交互。
- 跨企业、跨项目移动组织单元。
- 模板对既有项目的后台自动同步。
- 完整流程引擎及其配套运行时能力；如后续需要，单独编写 PRD 和技术契约。
- 组织单元、岗位和模板的物理删除。

## 十、当前确认门

产品方案与技术契约均已确认，当前按共享契约推进前后端实现、集成联调与 QA。若实施中发现会改变既有业务语义的缺口，必须先更新 PRD 与技术契约并重新确认，不能由实现阶段自行调整。
